// Itera cada elemento do Array;

const nums = [1, 4, 5, 2, 7, 2];

nums.forEach((num) => {
  console.log(num);
});

const nomes = ["Wagner", "Vitor", "Iracema", "Clarinda"];

nomes.forEach((nome) => {
  console.log(`O nome é: ${nome}`);
});
